#include "../fdf.h"
int hooks(int a, int b, int c, int d)
{
	if (i = '+' || i = '-')
	{
		if (i == '+')
		{
			data->offset = data->offset * 2;
			data->gap = data->gap * 2;
		}
		else
		{
			data->offset = data->offset / 2;
			data->gap = data->gap / 2;
		}
	}
	if (i == )
		data->elev = data->elev * 2;
	if (i == )
		data->evel = data->elev / 2;

	if (i == 'w')// move up
	if (i == 's')// move down
	if (i == 'a')// move left
	if (i == 'd')// move right
	if (i == 27)// esc gets out from the win and ctrl + c
	{
		exit (0);
	}
}